const SocialFeedEvent=require('../models/SocialFeedEvent');
const {notify}=require('./socialNotificationService');
async function publish({tipo,usuarioId,usuarioAlvoId=null,rankingPrivadoId=null,clubeId=null,titulo='',mensagem='',targetUrl='',visibilidade='publico',relevancia=0,metadata={}}){return SocialFeedEvent.create({tipo,usuarioId,usuarioAlvoId,rankingPrivadoId,clubeId,titulo,mensagem,targetUrl,visibilidade,status:'ativo',relevancia,metadata});}
async function publishRankingAchievement({usuarioId,nomeUsuario,posicao,escopo='geral',rankingPrivadoId=null}){let tipo=posicao===1?'RANKING_LEADER':'RANKING_TOP_ACHIEVED';const titulo=posicao===1?`@${nomeUsuario} assumiu a liderança`:`@${nomeUsuario} entrou no Top ${posicao}`;const e=await publish({tipo,usuarioId,rankingPrivadoId,titulo,mensagem:`Nova posição: ${posicao}º no ranking ${escopo}.`,targetUrl:'/ranking',relevancia:posicao<=3?5:4,metadata:{posicao,escopo}});await notify(usuarioId,{tipo,title:posicao===1?'Você assumiu a liderança!':`Você entrou no Top ${posicao}`,body:`Sua posição no ranking ${escopo} agora é ${posicao}º.`,targetUrl:'/ranking'});return e;}
module.exports={publish,publishRankingAchievement};
